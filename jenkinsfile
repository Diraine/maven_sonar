     pipeline {
        agent none
        stages {
         
          stage("build & sonarqub Analysis") {
            agent any
            steps {
              withSonarQubeEnv('sonar') {
                sh 'mvn clean package sonar:sonar'
              }
            }
          }
        }
      }
